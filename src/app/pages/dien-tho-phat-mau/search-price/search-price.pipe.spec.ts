import { SearchPricePipe } from './search-price.pipe';

describe('SearchPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPricePipe();
    expect(pipe).toBeTruthy();
  });
});
