import { SearchThanhSoPipe } from './search-thanh-so.pipe';

describe('SearchThanhSoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchThanhSoPipe();
    expect(pipe).toBeTruthy();
  });
});
