import { SearchOrganizationPipe } from './search-organization.pipe';

describe('SearchOrganizationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOrganizationPipe();
    expect(pipe).toBeTruthy();
  });
});
